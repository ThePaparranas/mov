const localStorageDefaultItems = {
  httpStatusBar: {
    enabled: true,
    onTop: true
  },
  collapsible: {
    caches: true,
    httpBar: true,
    logs: true
  },
  systemView: 'server'
}

export default localStorageDefaultItems
